Check port:
netstat -aof | findstr 5432

# Create Network
docker network create gameslibrary-network


# Rodar container Postgre na versão 16.3
docker run --name gameslibrarydb -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=gameslibrary -d --network gameslibrary-network postgres:16.3

# Rodar Pgadmin 4
docker run --name gameslibrarypgadmin4 -e PGADMIN_DEFAULT_EMAIL=admin@admin.com -e PGADMIN_DEFAULT_PASSWORD=admin -p 15432:80 -d --network gameslibrary-network dpage/pgadmin4:8.9